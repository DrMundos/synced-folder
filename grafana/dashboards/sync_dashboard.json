{
  "id": null,
  "title": "📊 Synced Folder Activity",
  "tags": ["sync", "postgres"],
  "timezone": "browser",
  "schemaVersion": 36,
  "version": 1,
  "panels": [
    {
      "type": "stat",
      "title": "Total Uploads",
      "datasource": "Postgres",
      "targets": [
        {
          "rawSql": "SELECT COUNT(*) FROM files_log WHERE action='upload';",
          "format": "table"
        }
      ],
      "gridPos": { "x": 0, "y": 0, "w": 6, "h": 4 }
    },
    {
      "type": "stat",
      "title": "Total Deletions",
      "datasource": "Postgres",
      "targets": [
        {
          "rawSql": "SELECT COUNT(*) FROM files_log WHERE action='delete';",
          "format": "table"
        }
      ],
      "gridPos": { "x": 6, "y": 0, "w": 6, "h": 4 }
    },
    {
      "type": "graph",
      "title": "Sync Activity Over Time",
      "datasource": "Postgres",
      "targets": [
        {
          "rawSql": "SELECT date_trunc('minute', created_at) AS time, COUNT(*) AS count FROM files_log GROUP BY time ORDER BY time;",
          "format": "time_series"
        }
      ],
      "gridPos": { "x": 0, "y": 4, "w": 12, "h": 8 },
      "lines": true,
      "fill": 3,
      "linewidth": 2
    }
  ]
}
